require 'spec_helper'

describe Rejoinder do
  it 'has a version number' do
    expect(Rejoinder::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
